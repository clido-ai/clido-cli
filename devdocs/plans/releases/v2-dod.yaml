# V2 Definition of Done — machine-readable checklist (detailed).
# Source plan: v2.md. Run scripts/verify-dod.sh when CURRENT is v2.
release: v2
source_plan: v2.md

items:
  # --- Build and test (inherited) ---
  - id: build
    description: "Workspace builds for all targets"
    source: "v2.md builds on V1.5"
    verification:
      type: command
      command: "cargo build --all-targets"
    status: GAP
    gap_reason: "V2 not yet released"

  - id: tests
    description: "All workspace tests pass"
    source: "v2.md Phase 9.1"
    verification:
      type: command
      command: "cargo test --workspace"
    status: GAP
    gap_reason: "V2 not yet released"

  - id: coverage-workspace
    description: "Workspace line coverage >= 75% (V2 floor)"
    source: "v2.md Exit Criteria; development-plan Phase 5.4.2"
    verification:
      type: coverage
      scope: workspace
      min_percent: 75
    status: GAP
    gap_reason: "V2 not yet released"

  # --- Phase 4.1 Multi-Provider (v2.md Exit Criteria) ---
  - id: provider-anthropic
    description: "Anthropic provider works behind ModelProvider trait"
    source: "v2.md Exit Criteria"
    verification:
      type: command
      command: "cargo test -p clido-providers anthropic"
    status: GAP
    gap_reason: "V2 not yet released"

  - id: provider-openai
    description: "OpenAI provider works behind ModelProvider trait"
    source: "v2.md Exit Criteria"
    verification:
      type: command
      command: "cargo test -p clido-providers openai"
    status: GAP
    gap_reason: "V2 not yet released"

  - id: provider-openrouter
    description: "OpenRouter provider works behind ModelProvider trait"
    source: "v2.md Exit Criteria"
    verification:
      type: command
      command: "cargo test -p clido-providers openrouter"
    status: GAP
    gap_reason: "V2 not yet released"

  - id: provider-switching
    description: "Provider switching via --profile / --provider / --model without editing config"
    source: "v2.md UX outcomes"
    verification:
      type: cli
      argv: ["--help"]
      expect_exit: 0
      expect_stdout_contains: "--provider"
    status: GAP
    gap_reason: "V2 not yet released"

  # --- Phase 4.2.4 Prompt Caching (v2.md Exit Criteria) ---
  - id: prompt-caching-anthropic
    description: "Prompt caching active on Anthropic; 20-turn session shows cache_read_input_tokens > 0; session cost reflects 10% rate for cached tokens"
    source: "v2.md Exit Criteria; Phase 4.2.4"
    verification:
      type: command
      command: "cargo test -p clido-providers"
    status: GAP
    gap_reason: "V2 not yet released"

  # --- Phase 6.1 Startup Performance (v2.md Exit Criteria) ---
  - id: startup-under-200ms
    description: "Startup completes in under 200ms to first API request"
    source: "v2.md Exit Criteria; Phase 6.1"
    verification:
      type: command
      command: "cargo test -p clido-cli"
    status: GAP
    gap_reason: "V2 not yet released"

  # --- Phase 6.3 Concurrent Provider Requests (v2.md Exit Criteria) ---
  - id: concurrent-provider-correctness
    description: "Concurrent provider requests do not regress sequential correctness"
    source: "v2.md Exit Criteria; Phase 6.3"
    verification:
      type: command
      command: "cargo test -p clido-agent"
    status: GAP
    gap_reason: "V2 not yet released"

  # --- Phase 6.4 File Read LRU Cache (v2.md Exit Criteria) ---
  - id: file-read-lru-cache
    description: "File read LRU cache reduces repeated read latency measurably"
    source: "v2.md Exit Criteria; Phase 6.4"
    verification:
      type: command
      command: "cargo test -p clido-tools"
    status: GAP
    gap_reason: "V2 not yet released"

  # --- Phase 7.1 Bash Sandboxing (v2.md Exit Criteria) ---
  - id: bash-sandbox-macos-linux
    description: "Bash execution can run in sandboxed mode on both macOS and Linux"
    source: "v2.md Exit Criteria; Phase 7.1"
    verification:
      type: command
      command: "cargo test -p clido-tools bash"
    status: GAP
    gap_reason: "V2 not yet released"

  - id: cli-sandbox-flag
    description: "clido --sandbox enables Bash sandboxing"
    source: "v2.md CLI Surface"
    verification:
      type: cli
      argv: ["--help"]
      expect_exit: 0
      expect_stdout_contains: "--sandbox"
    status: GAP
    gap_reason: "V2 not yet released"

  # --- Phase 7.3 Audit Logging (v2.md Exit Criteria) ---
  - id: audit-log-every-tool-call
    description: "Every tool call is recorded in an append-only audit log"
    source: "v2.md Exit Criteria; Phase 7.3"
    verification:
      type: command
      command: "cargo test -p clido-agent"
    status: GAP
    gap_reason: "V2 not yet released"

  - id: cli-audit
    description: "clido audit, audit --tail, --session, --tool, --since, --json exist"
    source: "v2.md CLI Surface"
    verification:
      type: cli
      argv: ["audit", "--help"]
      expect_exit: 0
    status: GAP
    gap_reason: "V2 not yet released"

  # --- Phase 9.3 Structured Telemetry (v2.md Exit Criteria) ---
  - id: telemetry-tool-durations
    description: "Structured telemetry records tool durations, provider calls, and retry events"
    source: "v2.md Exit Criteria; Phase 9.3"
    verification:
      type: command
      command: "cargo test -p clido-agent"
    status: GAP
    gap_reason: "V2 not yet released"

  # --- Phase 8.1 Hooks (v2.md Exit Criteria) ---
  - id: hooks-pretool-posttool
    description: "Hooks fire for PreToolUse and PostToolUse when configured"
    source: "v2.md Exit Criteria; Phase 8.1"
    verification:
      type: command
      command: "cargo test -p clido-agent"
    status: GAP
    gap_reason: "V2 not yet released"

  # --- Phase 8.5 Shell Completion and Man (v2.md Exit Criteria) ---
  - id: completions-bash-zsh
    description: "Shell completion works in bash and zsh"
    source: "v2.md Exit Criteria; Phase 8.5"
    verification:
      type: cli
      argv: ["completions", "bash", "--help"]
      expect_exit: 0
    status: GAP
    gap_reason: "V2 not yet released"

  - id: cli-completions-fish
    description: "clido completions fish exists"
    source: "v2.md CLI Surface"
    verification:
      type: cli
      argv: ["completions", "fish"]
      expect_exit: 0
    status: GAP
    gap_reason: "V2 not yet released"

  - id: cli-man
    description: "clido man exists (man page generation)"
    source: "v2.md CLI Surface"
    verification:
      type: cli
      argv: ["man", "--help"]
      expect_exit: 0
    status: GAP
    gap_reason: "V2 not yet released"

  # --- Phase 8.6 Live Plan (v2.md Exit Criteria) ---
  - id: live-plan-visible
    description: "Live plan or progress output is visible in interactive mode"
    source: "v2.md Exit Criteria; Phase 8.6"
    verification:
      type: command
      command: "cargo test -p clido-cli"
    status: GAP
    gap_reason: "V2 not yet released"

  # --- Phase 9.1 Full Test Coverage (v2.md Exit Criteria) ---
  - id: coverage-meets-targets
    description: "Test coverage meets targets in testing-strategy-and-master-test-plan.md"
    source: "v2.md Exit Criteria; Phase 9.1"
    verification:
      type: coverage
      scope: workspace
      min_percent: 75
    status: GAP
    gap_reason: "V2 not yet released"

  # --- Phase 9.2 Benchmarks (v2.md Exit Criteria) ---
  - id: benchmarks-baseline
    description: "Benchmarks are tracked and a regression baseline exists"
    source: "v2.md Exit Criteria; Phase 9.2"
    verification:
      type: command
      command: "cargo bench --no-run 2>/dev/null || true"
    status: GAP
    gap_reason: "V2 not yet released"

  # --- Phase 9.4 Documentation (v2.md Exit Criteria) ---
  - id: docs-installation-config-flags
    description: "Documentation covers installation, configuration, and all CLI flags"
    source: "v2.md Exit Criteria; Phase 9.4"
    verification:
      type: command
      command: "test -f README.md && test -f devdocs/plans/cli-interface-specification.md"
    status: GAP
    gap_reason: "V2 not yet released"

  # --- Phase 9.5 Packaging (v2.md Exit Criteria) ---
  - id: release-binary-installable
    description: "A release binary can be installed on a clean macOS and Linux system"
    source: "v2.md Exit Criteria; Phase 9.5"
    verification:
      type: command
      command: "cargo build --release -p clido-cli"
    status: GAP
    gap_reason: "V2 not yet released"

  # --- CLI Surface V2 (v2.md) ---
  - id: clido-run-subcommand
    description: "clido run <prompt> — explicit run subcommand"
    source: "v2.md CLI Surface"
    verification:
      type: cli
      argv: ["run", "--help"]
      expect_exit: 0
    status: GAP
    gap_reason: "V2 not yet released"

  - id: cli-input-format
    description: "clido --input-format text|stream-json for SDK/subprocess integration"
    source: "v2.md CLI Surface"
    verification:
      type: cli
      argv: ["--help"]
      expect_exit: 0
      expect_stdout_contains: "input-format"
    status: GAP
    gap_reason: "V2 not yet released"

  - id: cli-stats
    description: "clido stats, stats --session <id>, --json"
    source: "v2.md CLI Surface"
    verification:
      type: cli
      argv: ["stats", "--help"]
      expect_exit: 0
    status: GAP
    gap_reason: "V2 not yet released"

  # --- Phase 9.6 Production Hardening ---
  - id: production-hardening
    description: "Production hardening complete (security, packaging, CI)"
    source: "v2.md Included Scope Phase 9.6"
    verification:
      type: command
      command: "cargo test --workspace"
    status: GAP
    gap_reason: "V2 not yet released"

  # --- UX (ux-requirements.md; applies to all releases) ---
  - id: ux-first-run-init-copy
    description: "First-run and clido init prompts use copy from ux-requirements §2 (Type 1 or 2, press Enter, defaults in brackets)"
    source: "devdocs/plans/ux-requirements.md §2; CLI spec §4"
    verification:
      type: command
      command: "cargo test -p clido-cli --test integration init_prompts_contain_ux_copy"
    status: DONE

  - id: ux-script-intro-before-init
    description: "scripts/run-in-test-env.sh prints intro before clido init (ux-requirements §3.2)"
    source: "devdocs/plans/ux-requirements.md §3.2"
    verification:
      type: command
      command: "grep -q 'Clido will ask 2 questions' scripts/run-in-test-env.sh"
    status: DONE
